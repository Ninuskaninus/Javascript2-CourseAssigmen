import { fetchAllPosts } from "/js/API/get/allPosts.js";
import { fetchMyProfile } from "/js/API/get/myProfile.js";

export async function filterMyPosts() {
/*     const allPosts = await fetchAllPosts();
    const myProfile = await fetchMyProfile();
    const myUsername = myProfile.name.trim(); // Trim any leading/trailing whitespace
  
    console.log("My username:", myUsername);
  
    const myPosts = allPosts.filter((post) => {
      const postUsername = post.username.trim(); // Trim any leading/trailing whitespace
      return postUsername === myUsername;
    });
  
    console.log("My posts:", myPosts); */
  }
  
  
